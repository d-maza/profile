---
// Sección de formación académica y certificaciones
import educationEs from '../data/education-es.json';
import educationEn from '../data/education-en.json';
import i18nEs from '../data/i18n/es.json';
import i18nEn from '../data/i18n/en.json';

const allData = {
  es: { 
    i18n: i18nEs.education,
    formal: educationEs.formal,
    certifications: educationEs.certifications
  },
  en: { 
    i18n: i18nEn.education,
    formal: educationEn.formal,
    certifications: educationEn.certifications
  },
};

// Por defecto español
const i18n = i18nEs.education;
const { formal } = educationEs;
const certifications = educationEs.certifications;
---

<section id="formacion" class="py-20 relative">
  <div class="container mx-auto px-6">
    <!-- Título de la sección -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold mb-6">
        <span class="bg-gradient-to-r from-green-400 to-green-600 bg-clip-text text-transparent" data-i18n="education-title">
          {i18n.title}
        </span>
      </h2>
      <p class="text-xl text-gray-300 max-w-3xl mx-auto" data-i18n="education-subtitle">
        {i18n.subtitle}
      </p>
    </div>
    
    <div class="max-w-5xl mx-auto">
      <!-- Educación Formal -->
      <div class="mb-16">
        <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3">
          <div class="w-8 h-8 bg-gradient-to-r from-green-500 to-green-600 rounded-lg flex items-center justify-center">
            🎓
          </div>
          Educación Formal
        </h3>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          {formal.map((edu) => (
            <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-6 hover:border-green-400/40 transition-all duration-300">
              <div class="flex items-start justify-between mb-4">
                <div>
                  <h4 class="text-lg font-semibold text-white mb-2">{edu.degree}</h4>
                  <p class="text-green-400 font-medium">{edu.institution}</p>
                  <p class="text-gray-400 text-sm">{edu.period}</p>
                </div>
                <div class="bg-green-500/10 backdrop-blur-sm rounded-full p-2">
                  <span class="text-green-400 text-xs font-medium">{edu.status}</span>
                </div>
              </div>
              <p class="text-gray-300 text-sm">
                {edu.description}
              </p>
            </div>
          ))}
        </div>
      </div>
      
      <!-- Certificaciones -->
      <div>
        <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3">
          <div class="w-8 h-8 bg-gradient-to-r from-green-500 to-green-600 rounded-lg flex items-center justify-center">
            🏆
          </div>
          Certificaciones Profesionales
        </h3>
        
        <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-8">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <!-- CSS & JS -->
            <div>
              <h4 class="text-lg font-semibold text-green-400 mb-4">CSS & JS</h4>
              <ul class="space-y-2">
                {certifications.css_js.map((cert) => (
                  <li>
                    <a href={cert.link} target="_blank" class="text-gray-300 hover:text-green-400 transition-colors flex items-center gap-2 group">
                      <svg class="w-4 h-4 text-green-500 group-hover:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      <span>{cert.name}</span>
                    </a>
                  </li>
                ))}
              </ul>
            </div>

            <!-- NODE -->
            <div>
              <h4 class="text-lg font-semibold text-green-400 mb-4">NODE</h4>
              <ul class="space-y-2">
                {certifications.node.map((cert) => (
                  <li>
                    <a href={cert.link} target="_blank" class="text-gray-300 hover:text-green-400 transition-colors flex items-center gap-2 group">
                      <svg class="w-4 h-4 text-green-500 group-hover:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      <span>{cert.name}</span>
                    </a>
                  </li>
                ))}
              </ul>
            </div>

            <!-- TypeScript -->
            <div>
              <h4 class="text-lg font-semibold text-green-400 mb-4">TypeScript</h4>
              <ul class="space-y-2">
                {certifications.typescript.map((cert) => (
                  <li>
                    <a href={cert.link} target="_blank" class="text-gray-300 hover:text-green-400 transition-colors flex items-center gap-2 group">
                      <svg class="w-4 h-4 text-green-500 group-hover:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      <span>{cert.name}</span>
                    </a>
                  </li>
                ))}
              </ul>
            </div>

            <!-- PHP & Framework -->
            <div>
              <h4 class="text-lg font-semibold text-green-400 mb-4">PHP & Framework</h4>
              <ul class="space-y-2">
                {certifications.php.map((cert) => (
                  <li>
                    <a href={cert.link} target="_blank" class="text-gray-300 hover:text-green-400 transition-colors flex items-center gap-2 group">
                      <svg class="w-4 h-4 text-green-500 group-hover:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      <span>{cert.name}</span>
                    </a>
                  </li>
                ))}
              </ul>
            </div>

            <!-- ANGULAR -->
            <div>
              <h4 class="text-lg font-semibold text-green-400 mb-4">ANGULAR</h4>
              <ul class="space-y-2">
                {certifications.angular.map((cert) => (
                  <li>
                    <a href={cert.link} target="_blank" class="text-gray-300 hover:text-green-400 transition-colors flex items-center gap-2 group">
                      <svg class="w-4 h-4 text-green-500 group-hover:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      <span>{cert.name}</span>
                    </a>
                  </li>
                ))}
              </ul>
            </div>

            <!-- Others -->
            <div>
              <h4 class="text-lg font-semibold text-green-400 mb-4">Others</h4>
              <ul class="space-y-2">
                {certifications.others.map((cert) => (
                  <li>
                    <a href={cert.link} target="_blank" class="text-gray-300 hover:text-green-400 transition-colors flex items-center gap-2 group">
                      <svg class="w-4 h-4 text-green-500 group-hover:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      <span>{cert.name}</span>
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Skills destacados -->
      <div class="mt-16">
        <h3 class="text-2xl font-bold text-white mb-8 flex items-center gap-3">
          <div class="w-8 h-8 bg-gradient-to-r from-green-500 to-green-600 rounded-lg flex items-center justify-center">
            ⚡
          </div>
          <span data-i18n="skills-title">Habilidades Principales</span>
        </h3>
        
        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
          <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-4 text-center hover:border-green-400/40 transition-all duration-300">
            <div class="flex items-center justify-center mb-2 h-8">
              <img src="/angular.svg" alt="Angular" class="w-8 h-8" />
            </div>
            <div class="text-sm text-white font-medium">Angular</div>
          </div>
          <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-4 text-center hover:border-green-400/40 transition-all duration-300">
            <div class="flex items-center justify-center mb-2 h-8">
              <img src="/nuxt.svg" alt="Nuxt" class="w-8 h-8" />
            </div>
            <div class="text-sm text-white font-medium">Nuxt</div>
          </div>
          <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-4 text-center hover:border-green-400/40 transition-all duration-300">
            <div class="flex items-center justify-center mb-2 h-8">
              <img src="/nestjs.svg" alt="NestJS" class="w-8 h-8" />
            </div>
            <div class="text-sm text-white font-medium">NestJS</div>
          </div>
          <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-4 text-center hover:border-green-400/40 transition-all duration-300">
            <div class="flex items-center justify-center mb-2 h-8">
              <img src="/typescript.svg" alt="TypeScript" class="w-8 h-8" />
            </div>
            <div class="text-sm text-white font-medium">TypeScript</div>
          </div>
          <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-4 text-center hover:border-green-400/40 transition-all duration-300">
            <div class="flex items-center justify-center mb-2 h-8">
              <img src="/laravel.svg" alt="Laravel" class="w-8 h-8" />
            </div>
            <div class="text-sm text-white font-medium">Laravel</div>
          </div>
          <div class="bg-dark-800/50 backdrop-blur-sm border border-green-500/20 rounded-lg p-4 text-center hover:border-green-400/40 transition-all duration-300">
            <div class="flex items-center justify-center mb-2 h-8">
              <img src="/spring.svg" alt="Spring Boot" class="w-8 h-8" />
            </div>
            <div class="text-sm text-white font-medium">Spring Boot</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script define:vars={{ allData }}>
  function updateEducation() {
    const lang = localStorage.getItem('lang') || 'en';
    const data = allData[lang];
    
    if (!data) return;
    
    const titleEl = document.querySelector('[data-i18n="education-title"]');
    const subtitleEl = document.querySelector('[data-i18n="education-subtitle"]');
    const skillsTitleEl = document.querySelector('[data-i18n="skills-title"]');
    
    if (titleEl) titleEl.textContent = data.i18n.title;
    if (subtitleEl) subtitleEl.textContent = data.i18n.subtitle;
    if (skillsTitleEl && data.i18n.skills) skillsTitleEl.textContent = data.i18n.skills.title;
  }
  
  window.addEventListener('languageChanged', () => {
    updateEducation();
  });
  
  document.addEventListener('DOMContentLoaded', updateEducation);
  if (document.readyState !== 'loading') {
    updateEducation();
  }
</script>